name: Docker Build and Push

on:
  push:
    branches:
      - main

permissions:
  contents: read
  packages: write

jobs:
  docker:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Backend Image
        run: |
          docker build \
            -t ghcr.io/${{ github.repository_owner }}/vehicle-rental-backend:latest \
            ./backend

      - name: Push Backend Image
        run: |
          docker push \
            ghcr.io/${{ github.repository_owner }}/vehicle-rental-backend:latest

      - name: Build Frontend Image
        run: |
          docker build \
            -t ghcr.io/${{ github.repository_owner }}/vehicle-rental-frontend:latest \
            ./frontend

      - name: Push Frontend Image
        run: |
          docker push \
            ghcr.io/${{ github.repository_owner }}/vehicle-rental-frontend:latest
            